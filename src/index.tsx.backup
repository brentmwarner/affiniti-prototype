import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { LeadProfilePage } from "./screens/LeadProfilePage";

createRoot(document.getElementById("app") as HTMLElement).render(
  <StrictMode>
    <LeadProfilePage />
  </StrictMode>,
);
